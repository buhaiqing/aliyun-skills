# SLB Operations — AIOps Prompts Handbook

> **Purpose:** 30+ categorized prompt examples for SLB with AIOps focus.

## 指标查询类

1.查询 SLB 实例 lb-xxx 过去 1 小时的 5xx 错误率"
2.查看 SLB 后端响应时间（InstanceRt）趋势"
3."批量查询所有 SLB 实例的连接数和健康检查状态"

## 健康检查与 5xx 诊断

4."SLB 5xx 突然飙升，帮我定位是哪些后端有问题"
5."后端健康检查频繁失败，帮我排查根因"
6."SLB 4xx 异常突增，帮我区分是客户端错误还是后端配置问题"

## 告警驱动的诊断

7."收到 SLB 带宽告警，帮我跨 Skill 诊断"
8."多个 SLB 实例同时告警，是否有关联？"
9."SLB 丢连接异常，帮我诊断是否后端服务不可用"
10."SLB 延迟（InstanceRt）超过 5 秒，帮我排查"

## 多指标关联巡检

11.执行 SLB 多指标巡检，检查是否有异常模式匹配"
12."检查当前是否有 SLB 实例处于 5xx 异常风暴"
13."查看所有 SLB 的带宽饱和风险"
14."巡检 SLB 后端健康状态和连接趋势"

## 告警风暴处理

15."多个 SLB 同时告警，帮我聚合分析根因"
16."SLB 告警持续 10 分钟未恢复，是否进入了告警风暴？"
17."从多个 SLB 告警中识别出级联模式"

## 跨 Skill 协同诊断

18.告警驱动诊断：SLB 5xx + ECS + RDS 的跨 Skill 联合诊断"
19."SLB 5xx 告警触发，按决策树执行诊断流程"
20."SLB 健康检查失败，联动 ECS 和 VPC 诊断"

## 主动巡检

21."对所有 SLB 实例执行主动巡检"
22."生成 SLB 巡检报告，包括带宽、连接、后端健康"
23."巡检 SLB 健康检查配置是否合理"
24."巡检所有 SLB 的后端 5xx 率和 RT 趋势"

## 知识匹配

25."我的 SLB 故障模式匹配了哪个知识库 Pattern？"
26."根据当前 SLB 告警，匹配知识库并给出诊断建议"

## 可观测性联动

27."SLB 延迟突增，帮我查询 SLS 中的慢请求日志"
28."SLB 5xx 异常，帮我查 ARMS Trace 定位具体服务"
29."SLB 流量异常，通过 FlowLog 分析流量模式"

## 自愈操作

30."帮我自动修复 SLB 后端健康检查失败问题"
31."帮我调整 SLB 监听器配置优化延迟"
32."帮我将异常后端移出 SLB，等待修复后重新加入"
