# ECS Operations — Prompts Handbook

> **Purpose:** 25+ categorized prompt examples for ECS operations.

## 指标查询类 (≥3)

1. "查询 ECS 实例 i-bp1xxx 过去 1 小时的 CPU 使用率"
2. "查看这台实例最近 24 小时的内存和 CPU 趋势"
3. "批量查询所有 ECS 实例的当前 CPU 利用率"

## 实例管理 (≥3)

4. "帮我创建一台 ECS 实例，规格 4C8G，在 cn-hangzhou-b 可用区"
5. "启动/停止/重启这台 ECS 实例"
6. "查看我有多少台 Running 状态的 ECS"

## 云盘管理 (≥3)

7. "给这台实例添加一块 100GB 的云盘"
8. "卸载并删除这块云盘"
9. "查看所有云盘的使用率和容量"

## 安全组 (≥3)

10. "给实例添加安全组规则，允许 80 和 443 端口入站"
11. "查看这个安全组的所有规则"
12. "检查这台实例的安全组是否允许访问"

## 诊断类 (≥3)

13. "这台 ECS 连不上 SSH，帮我排查一下"
14. "实例 CPU 突然 100% 了，帮我分析一下原因"
15. "内存一直在增长，怀疑内存泄漏，帮我确认"

## 多指标关联巡检 (≥2)

16. "执行 ECS 多指标巡检，检查 i-bp1xxx 的异常模式"
17. "检查当前是否有 ECS 实例处于 CPU-Memory 双高压状态"
18. "查看所有 ECS 实例的异常模式匹配情况"

## 告警驱动诊断 (≥3)

19. "收到 ECS CPU 告警，帮我诊断根因"
20. "ECS 实例 NotReachable 了，帮我排查"
21. "跨 Skill 诊断：SLB 5xx 和 ECS 是否有关联"

## 主动巡检 (≥2)

22. "对所有 ECS 实例执行主动巡检"
23. "生成 ECS 巡检报告，包括 CPU、内存、磁盘使用率"
24. "巡检所有 ECS 实例的安全组规则是否有异常开放"
