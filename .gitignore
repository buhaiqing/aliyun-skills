# ===== Go (Primary Runtime) =====

# JIT Go workspace (temporary, generated on-demand)
/tmp/go-runtime/
/tmp/go-workspace/
/tmp/go-cache/
/tmp/go-modcache/
/tmp/aliyun-sdk-workspace/

# Go build artifacts
*.exe
*.exe~
*.dll
*.so
*.dylib
*.test
*.out
go.sum

# Go workspace files (project-level, keep go.mod if exists)
# go.mod  # Keep if we add project-level go.mod

# ===== Credentials (CRITICAL) =====

# Alibaba Cloud CLI config
.aliyun/

# JD Cloud CLI config
.jdc/


# Environment files (NEVER commit secrets)
.env
.env.*
!.env.example

# ===== Python (Development tools only) =====

# Virtual environments
.venv/
venv/
env/
__pycache__/
*.py[cod]
*$py.class

# Distribution
dist/
build/
*.egg-info/
*.egg

# IDE/Editor
.idea/
.vscode/
*.swp
*.swo
*~

# ===== OS Generated =====

# macOS
.DS_Store
.AppleDouble
.LSOverride

# Windows
Thumbs.db
ehthumbs.db

# Linux
*~

# ===== Logs & Temporary =====

*.log
*.tmp
*.temp
.cache/

# ===== Project Specific =====

# Compiled skill binaries (future distribution)
# *.aliyun-sdk-*  # Keep pre-compiled binaries if we distribute them

.trae/
.agents/
.omc/
.omo/
.omp/
.pi/
infra-baseline/
output/

# GCL trace artifacts (per AGENTS.md §12.6; sanitized traces may still
# leak operational context, so treat all traces as ephemeral by default)
audit-results/

# Operational reports (contain instance IDs, business context — do NOT commit)
report.md
report-*.md
rds-waste-report.md
rds-*-report.md

# ===== Runtime Data Root (Sprint 18) =====
# 统一运行时数据根目录 (默认 ${SKILLS_DIR}/.runtime, 可通过环境变量 ALIYUN_SKILLS_RUNTIME_ROOT 覆盖)
# 包含 baseline/audit/cache/logs/tmp 等子目录
.runtime/
.runtime-*/

# Legacy paths (Sprint 18 之前, 现在用软链接兼容 → .runtime/)
# 保留这些规则避免误提交历史数据
infra-baseline/
audit-results/

# Skill-internal runtime data (legacy; Sprint 19+ 将逐步迁移到 .runtime/)
alicloud-*/audit-results/
alicloud-*/scripts/audit-results/
alicloud-*/**/audit-results/

# Python cache (跨 skill 通用)
__pycache__/
*.py[cod]
*$py.class
alicloud-*/**/__pycache__/

# Test cache
.pytest_cache/