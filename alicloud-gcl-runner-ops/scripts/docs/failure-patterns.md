# Failure Patterns — Reflexion Memory

> **Purpose**: Structured failure memory extracted from GCL traces and Self-Review records.
> Agents can optionally load this file during Pre-flight to prevent known errors.
>
> **Auto-generated**: This file is regenerated by ``gcl_reflexion.py report``.
> **Token budget**: ≤ 200 lines. When exceeded, prune with ``gcl_reflexion.py maintain``.

---

## CLI Parameter Errors

> 从 GCL trace 中提取的 CLI 参数错误模式。高频模式优先。

|Skill|Command|Error Pattern|Root Cause|Fix|Count|Last Seen|
|---|---|---|---|---|---|---|
|ecs-ops|test|E1|RC|F1||2026-06-26T08:30:50.554577Z|

---

## Usage Guidelines

### For Agents (Pre-flight)

```
# Optional: Load failure patterns before executing a skill
# 1. Read this file (lazy-load, ~150 lines)
# 2. Filter patterns by current skill name
# 3. Inject relevant patterns into Generator context as prevention hints
```

### For GCL Runner

```
# After each GCL run with SAFETY_FAIL, the runner stores patterns:
#   reflexion_store(reflexion_extract(trace))
# Regenerate this file:
#   python gcl_reflexion.py report
```

### Maintenance

```
# Prune patterns with count < 3 if lines > 200:
#   python gcl_reflexion.py maintain --apply
```
